/**
 * KPRFlow Enterprise - React Native Entry Point
 * New Architecture with Design System
 */

import { AppRegistry } from 'react-native'
import App from './App'
import { name as appName } from './package.json'

AppRegistry.registerComponent(appName, () => App)
